with open ('../data/07.txt') as file:
    inputs = [_ for _ in file.read().strip().split('\n')]

#AND: &, OR: |, NOT: ~, XOR: ^, RSHIFT: >>, LSHIFT: <<

for j in inputs:
    cmd = j.split(' ')

# NOT DONE

    
